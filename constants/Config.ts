/**
 * App configuration and feature flags
 */

export const Config = {
  // Feature flags
  features: {
    // Set to false to disable the Home tab
    homeTabEnabled: false,
  },
  
  // App settings
  app: {
    name: 'bananastand',
    version: '1.0.0',
  },
};
